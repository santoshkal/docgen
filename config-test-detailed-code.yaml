# Test Configuration for Detailed Code-Block Explanation with Chunking
source:
  mode: single
  source_files: ../TDAS-MINDISTCALC.c74
  program_name: MINDISTCALC

output:
  metadata_dir: ../metadata
  docs_path: ../docs

llm:
  provider: openai
  model: gpt-4.1
  api_key: ${OPENAI_API_KEY}
  temperature: 0.1

source_extraction:
  enabled: true
  compress: false

logging:
  level: info
  verbose: true
